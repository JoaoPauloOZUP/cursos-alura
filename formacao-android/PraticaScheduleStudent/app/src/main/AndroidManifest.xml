<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zupacademy.praticaschedulestudent">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PraticaScheduleStudent"
        android:name=".ScheduleApplication" >
        <activity android:name=".ui.activity.CreateFormStudent" />
        <activity android:name=".ui.activity.EditFormStudent" />
        <activity
            android:exported="true"
            android:name=".ui.activity.ListStudentsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>