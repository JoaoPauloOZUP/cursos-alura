<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activity.FormCreateStudent">

    <EditText
        android:id="@+id/form_students_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:hint="@string/name"
        android:inputType="textCapWords"
        android:importantForAutofill="no"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <EditText
        android:id="@+id/form_students_phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:hint="@string/phone"
        android:inputType="phone"
        android:importantForAutofill="no"
        app:layout_constraintStart_toStartOf="@+id/form_students_name"
        app:layout_constraintEnd_toEndOf="@id/form_students_name"
        app:layout_constraintTop_toBottomOf="@id/form_students_name"/>

    <EditText
        android:id="@+id/form_students_email"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:hint="@string/email"
        android:inputType="textEmailAddress"
        android:importantForAutofill="no"
        app:layout_constraintStart_toStartOf="@id/form_students_phone"
        app:layout_constraintEnd_toEndOf="@id/form_students_phone"
        app:layout_constraintTop_toBottomOf="@id/form_students_phone"/>

</androidx.constraintlayout.widget.ConstraintLayout>